# Plumbline Project Template — SUPERSEDED

> **This template has been consolidated into
> [`Plumbline-Studio/plumbline-template`](https://github.com/Plumbline-Studio/plumbline-template).
> Start all new repos there.**

Everything this repo held now lives in the consolidated template, with the
engineering layer it was missing:

- `WORTHINESS.md` (the build gate) — moved verbatim
- The conditioning `CLAUDE.md` content (operating principles, plan-of-record links,
  stack defaults) — merged into the consolidated `CLAUDE.md`
- The PR gate checklist — merged into a fuller PR template (what/why/how-to-verify)

What a repo born here was silently missing — and gets there: the surface-keyed CI
gate, contracts/, playscripts/, `.claude/` hooks + reviewer subagent, and the
`agent_docs/` style layer + Clean Code library.

New-project motion (unchanged except the template source): plan folder in
`tool-maker/projects/` → numbered K Drive folder → pipeline record → **repo from
`plumbline-template`** → fill CLAUDE.md brackets → complete WORTHINESS.md before
build.

Build it true.
